module top3_3(clk,LED);
    input clk;
    output [2:0]LED;
    
    wire clk_temp;   //用于连接
    
    divider d1(.clk(clk),      .clk_N(clk_temp));
    counter c1(.clk(clk_temp), .out(LED[2:0]));
    
endmodule